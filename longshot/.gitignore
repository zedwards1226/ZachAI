# Secrets
.env

# Runtime state
state/*.json
state/*.lock
state/*.db
state/*.db-*
*.pid

# But DO commit the band-tuning artifacts (config, not runtime data)
!state/strategy_bands.json
!state/band_history.jsonl

# Logs
logs/*.log
logs/*.log.*

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# Data — historical pulls can be huge, never commit
data/raw/
data/processed/
*.parquet
*.csv

# IDE
.idea/
.vscode/
.DS_Store

# Dashboard build output (regenerated by npm run build)
dashboard/backend/static/
dashboard/frontend/node_modules/
dashboard/frontend/dist/

# Backtest output
backtest/results/
